export const Host = () => {
  /**
   * Default state: create lobby and load
   * Error states:
   *   - lobby couldn't be created
   *   - error
   * Loading state: loader
   * Success state: lobby code, lobby participant list, start button
   */
};
