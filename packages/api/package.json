{
  "name": "@movie/api",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules coverage",
    "type-check": "tsc --noEmit",
    "watch": "pnpm with-env vitest watch",
    "unit": "pnpm with-env vitest run",
    "test": "pnpm with-env vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "pnpm lint --fix",
    "with-env": "cross-env dotenv -e ../../.env --"
  },
  "dependencies": {
    "@movie/auth": "workspace:*",
    "@movie/db": "workspace:*",
    "@paralleldrive/cuid2": "^2.2.2",
    "@trpc/server": "^10.45.0",
    "ioredis": "^5.3.2",
    "superjson": "^2.2.1",
    "tslog": "^4.9.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@movie/eslint-config": "workspace:*",
    "@movie/tsconfig": "workspace:*",
    "cross-env": "^7.0.3",
    "dotenv-cli": "^7.3.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0",
    "vitest-mock-extended": "^1.3.1"
  }
}
