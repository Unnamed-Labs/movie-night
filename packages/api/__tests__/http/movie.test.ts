import { describe } from 'vitest';
// import { createInnerTRPCContext } from '../../index';
// import { createCaller } from '../helpers/caller';
// import { prismaMock } from '../helpers/singleton';

// const movie = {
//   id: '1',
//   title: 'Shrek',
//   description: 'A good movie.',
//   runtime: '90mins',
//   score: 0.99,
//   location: 'US',
//   date: '05/31/2007',
//   imageSrc: 'shrek.jpg',
//   imageAlt: 'Shrek movie poster',
//   ratingId: '1',
//   createdDate: new Date(),
// };

describe.skip('movie', () => {
  // const ctx = createInnerTRPCContext({ session: null });
  // const caller = createCaller(ctx);
  // beforeAll(() => {
  //   prismaMock.movie.create.mockResolvedValue(movie);
  // });
  // test('get the most popular movies', async () => {
  //   const popular = await caller.movie.getPopular();
  //   expect(popular.length).toEqual(1);
  // });
  // test.skip('find a movie if the name exists', () => {});
  // test.skip('find no movie if the name does not exist', () => {});
});
